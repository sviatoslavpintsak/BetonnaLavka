<p style="color: green"><%= notice %></p>

<h1>Products</h1>
  
<div id="products" class="d-flex justify-content-around row">
  <% @products.each do |product| %>
   <div class="col-sm-6 col-md-4 col-lg-3 mb-4" >
    <%= render product %>
    <p>
      <%= link_to "Show this product", product %>
    </p>
  
    </div>
  <% end %>
</div>  
</div>

  <%if current_user.present? && current_user.email == "sviatoslavpintsak@gmail.com"%>
    <%= link_to "New product", new_product_path %>
  <%end%>
